<Page
    x:Class="Classify.WinUI.Views.CalendarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Classify.WinUI.Models"
    mc:Ignorable="d">

    <Grid Padding="32" ColumnSpacing="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <CalendarView x:Name="Planner"
                      SelectedDatesChanged="OnSelectedDatesChanged"
                      SelectionMode="Single" />

        <StackPanel Grid.Column="1" Spacing="12">
            <TextBlock Text="Révisions" FontSize="20" FontWeight="SemiBold" />
            <ListView x:Name="EventsList" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:RevisionEvent">
                        <Grid Padding="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                            <StackPanel Spacing="4">
                                <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" />
                                <TextBlock Text="{x:Bind Date, Mode=OneWay}" Opacity="0.6" />
                                <ToggleSwitch Header="Terminé" IsOn="{x:Bind IsDone, Mode=TwoWay}" Toggled="OnEventToggled" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Button Content="Ajouter" Click="OnAddEventClick" />
        </StackPanel>
    </Grid>
</Page>
